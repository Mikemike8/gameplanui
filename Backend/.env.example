DB_HOST=localhost
DB_PORT=5432
DB_NAME=gameplan
DB_USER=postgres
DB_PASSWORD=postgres

# Optional explicit SQLAlchemy URL override (auto-built from vars if unset)
# DATABASE_URL=postgresql+psycopg2://postgres:postgres@localhost:5432/gameplan
